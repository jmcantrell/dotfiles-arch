#!/usr/bin/env sh

# Install and configure development tools.

set -e

sudo pacman -Sy --noconfirm --needed \
    git highlight tidy jq xmlstarlet \
    nodejs npm python python-pip vim neovim ctags

enable-node
enable-python
enable-vim
