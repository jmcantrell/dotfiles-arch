#!/usr/bin/env bash

# Configure localhost as a VirtualBox guest.

set -eu

sudo pacman --sync --noconfirm --needed virtualbox-guest-utils-nox
sudo systemctl --now enable vboxservice.service
sudo modprobe --all vboxguest vboxsf vboxvideo
sudo gpasswd --add "$USER" vboxsf
