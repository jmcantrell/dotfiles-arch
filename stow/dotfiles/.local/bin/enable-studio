#!/usr/bin/env bash

# Install and configure a multimedia studio.

has xinit || exit 0

install-krita

sudo pacman -S --noconfirm --needed \
	audacity ardour \
	gimp gimp-help-en darktable \
	inkscape synfigstudio \
	blender mypaint mtpaint \
	obs-studio scribus \
	gpick tiled
