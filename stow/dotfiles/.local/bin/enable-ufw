#!/usr/bin/env bash

# Install and configure a simple network firewall.

set -e

sudo pacman -S --noconfirm --needed ufw ufw-extras
sudo systemctl --now enable ufw.service

sudo ufw logging off
sudo ufw default deny
sudo ufw enable

ufw status
