#!/usr/bin/env sh

# Install and configure sound support and tools.

set -e

pacman="sudo pacman -S --noconfirm --needed"

$pacman pulseaudio pulseaudio-{alsa,zeroconf} alsa-utils pamixer

has xinit || exit 0

$pacman paprefs pavucontrol libcanberra
