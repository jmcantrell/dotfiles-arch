#!/usr/bin/env bash

# Install and configure sound support and tools.

set -e

sudo pacman -S --noconfirm --needed \
	pulseaudio pulseaudio-{alsa,zeroconf} alsa-utils pamixer

has xinit || exit 0

sudo pacman -S --noconfirm --needed \
	paprefs pavucontrol libcanberra
