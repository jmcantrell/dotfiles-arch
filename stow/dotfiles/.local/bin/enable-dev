#!/usr/bin/env bash

# Install and configure development tools.

set -e

sudo pacman -S --noconfirm --needed \
    vim neovim ctags git words aspell-en \
    highlight tidy dos2unix jq markdown xmlstarlet \
    nodejs npm python python-pip


enable-node
enable-python
enable-vim
