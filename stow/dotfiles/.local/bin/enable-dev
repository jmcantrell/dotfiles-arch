#!/usr/bin/env bash

# Install and configure development tools.

set -e

sudo pacman -S --noconfirm --needed \
    neovim ctags git words aspell-en \
    highlight tidy dos2unix jq markdown xmlstarlet \
    nodejs python

enable-pyenv && $SHELL -c enable-python
enable-nvm && $SHELL -c enable-node
