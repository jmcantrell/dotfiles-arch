#!/usr/bin/env bash

# Enable support for 32-bit applications.

set -e

# detect x86_64 processor
grep -wq lm /proc/cpuinfo || exit 0

# enable multilib for pacman
sudo sed -i '/^#\?\[multilib\]/{ N; s/#//g; }' /etc/pacman.conf

# needs an update before first use
sudo pacman -Sy
