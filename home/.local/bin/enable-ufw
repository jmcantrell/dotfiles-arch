#!/usr/bin/env bash

# Install and configure a firewall.

set -eu

sudo pacman --sync --noconfirm --needed ufw
sudo systemctl enable --now ufw.service

sudo ufw default deny

sudo ufw allow ssh
sudo ufw allow mdns
sudo ufw allow 49152:65535/tcp

sudo ufw enable
